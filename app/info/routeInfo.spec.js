'use strict';

describe('app.routeInfo module', function() {

    beforeEach(module('app.routeInfo'));

    describe('app.routeInfo', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewCtrl = $controller('RouteInfoCtrl');
            expect(viewCtrl).toBeDefined();
        }));

    });
});